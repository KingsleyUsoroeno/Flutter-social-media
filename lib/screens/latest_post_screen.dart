import 'package:flutter/material.dart';

class LatestPostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Latest post suckers'),
      ),
    );
  }
}
